If you are interested in developing `ilamb`, we welcome contributions! Here, we provide guides for creating a clone of `ilamb`, setting up the `ilamb` development environment, and contributing to the project. We first recommend [installing](https://docs.astral.sh/uv/getting-started/installation/) `uv`, which is a Python package manager that we use to manage our development environment.