# Installing `ilamb3`

```{warning} Unfinished
This feature is not in its final form. We will provide some details here, but expect this to change in the future.
```

The `ilamb3` package is pure-python and available on PyPI. To install it, simply:

```bash
python -m pip install ilamb3
```
